
node {
    environment {
        BRANCH_NAME = 'main'
    }
    stage('Hello') {
        echo "le nom de la branche est $BRANCH_NAME"
        if (env.BRANCH_NAME == 'main') {
            echo 'Hello Manuel!'
        } 
        else {
            echo 'Hello Enol!'
            }
        } 
}
