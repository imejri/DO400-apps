
String BRANCH_NAME = 'main'
node {  
    stage('Hello') {
        echo "le nom de la branche est $BRANCH_NAME"
        if (${BRANCH_NAME} == 'main') {
            echo 'Hello Manuel!'
        } 
        else {
            echo 'Hello Enol!'
            }
        } 
}
