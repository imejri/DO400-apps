
node {
    stage('Hello') {
        def BRANCH_NAME='main'
        echo "le nom de la branche est $BRANCH_NAME"
        if (env.BRANCH_NAME == 'main') {
            echo 'Hello Manuel!'
        } 
        else {
            echo 'Hello Enol!'
            }
        } 
}
